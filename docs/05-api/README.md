# API Contract

`contracts/http/openapi.yaml` is the canonical machine-readable HTTP contract. Generated TypeScript bindings live at `src/generated/openapi.ts`.

Latest verified Step-44 lineage: 513 paths / 582 operations / 1138 local references, PASS.

Run:

```bash
pnpm contract:validate
pnpm contract:types
```

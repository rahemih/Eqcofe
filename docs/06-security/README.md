# Security Architecture

Security controls include customer/staff authentication boundaries, permission RBAC, Step-Up for critical admin operations, idempotency, rate/session/CORS controls, transactional audit/outbox semantics and fail-closed external providers.

Production hardening (CSP, deployment secret management, backup/restore, final rate-limit/session/database-privilege scenarios) remains planned in the production-preparation roadmap.

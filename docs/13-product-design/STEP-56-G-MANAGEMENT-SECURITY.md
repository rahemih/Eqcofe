# Step 56-G — Finance, Analytics, Configuration and Security

G design acceptance PASS; canonical completion requires exact-head Canonical CI and Phase A, expected-head merge and post-merge PASS. Baseline `e5e1c34744e94cc7e8740fe7f3927a071a021ef5` is F merge PR159, exact head `8e92f6b2e6e843edfa5e15b388facf36c3de5270`, Canonical CI34230018229 and post34230247029 PASS. Stop after G; H NOT_STARTED.

## Scope and authority

34 inherited G surfaces and185 operations retain exact A actors, journeys, domains, permissions, gaps, states and per-operation provenance.91 supported operations are designable;94 remain NO_ACTION. Profit-rule preview and RBAC writes remain blocked even where controller evidence exists. G15 integrations, G18–20 approvals/security events/incidents, G22–34 secrets/vulnerabilities/operations/data governance remain unavailable; they have conceptual recovery views, no reads, commands or successful outcomes. Security sessions G21 and staff G17 use only individually supported operations. No new permission, API, runtime, migration, dependency or business rule.

Authority is repository main, A machine contract, B shell, F handoff, current OpenAPI, controller decorators, application services/repositories and scope policy.90 hashed source files are pinned. Historical A–F snapshots remain immutable. Figma optional, no paid service or invented brand asset.

## Source-specific acceptance

| Surface | Behavior and boundary |
|---|---|
| G01 dashboard | Read aggregation only; signed integer Toman. No invented cash movement or uniform scope guarantee. |
| G02 journals | Draft2..500 lines; each exactly one positive debit/credit side, safe integer totals equal, accounts active/postable. Description1..1000, occurred_at no later than now+5min. Posting draft requires reason1..2000 and revalidation. Reverse posted once creates compensating posted entry; original is not deleted or directly edited. |
| G03 accounts | Create code ASCII1..40/name1..200/type/normal balance, optional parent/system key, postable. PATCH only name/parent/postable/active, at least one change. Self-parent rejected; deep cycle protection not certified. Deactivate has Step-Up; PATCH is_active does not inherit that separate route's Step-Up. |
| G04 costs | Nonnegative integer Toman, effect sign separate; four treatments deduct_before_profit_split/capitalized_into_cost/non_distributable_cost/informational_only. Item requires order; campaign and order exclusive. Capitalization type/id pair only for capitalized cost; finalization requires pair. Finalize draft; reason code80/reason2000/note4000 optional. Reverse original finalized once through negative-effect compensating record, amount stays positive. |
| G05 profit | Recalculate yields a new expected/provisional calculation, not finalization. Finalize requires fresh provisional fingerprint, paid/late_received/refunded payment, no unresolved refund and committed=succeeded, no draft costs and unambiguous single rule across items. Existing final is idempotent. Reverse current final with finalized distribution once; signed compensating profit/shares, no payout. |
| G06 rules | Global/category/brand/product, scope id required outside global, priority integer within±1000000. Owner percentages up to4 decimals sum exactly100. Required start, optional ordered end. Full input draft-only update; activate draft, expire active after start and no later than now+5min, reason required. List default500/max1000; preview stays NO_ACTION. Signed integer split conserves base. |
| G07 distributions | Read distribution and compensating reversal evidence; no settlement/payment action. |
| G08 financial reports | Catalog finance_summary/profit_by_order/costs_by_type/owner_distribution/journal_trial_balance. Ordered optional dates, limit default200/max1000. Run synchronously calculates and saves completed job. Cancel queued/running only; normal completed run cannot cancel. Export CSV/JSON from completed job. Download is JSON envelope with filename/mime/format/content; no requester binding, readiness filter at export lookup, private cache headers or CSV formula neutralization guarantee. Download has separate permission and Step-Up, explicit user action, no automatic opening/sending. |
| G09 analytics | Six read projections with sourceWatermark; stale labels, integer Toman and margin basis points. Operations from<=to<=asOf and limit<=500. Read projections are not guaranteed live source data or repair controls. |
| G10 analytic exports | Nine datasets: sales_revenue_daily, profit_daily, inventory_snapshot, customer_lifetime, wholesale_applications, fulfillment_operations, shipment_operations, return_operations, warranty_operations. CSV/JSON, owner-bound list/get/download, completed content only. Actor/key/payload-bound idempotency8..200; failed replay does not rerun job. Allowlisted columns/scalars, formula neutralization,5MiB limit,1..500 rows(default500), hash/row count/watermark. Attachment download nosniff and private,no-store; exact permission and Step-Up. These guarantees do not apply to G08. |
| G11 tax | Basis points integer0..10000, global/brand/category/product and scope id, name>=2, optional dates. Create draft; PATCH draft name/rate/dates only, immutable scope and If-Match. Activate draft If-Match/Step-Up, same-scope active interval overlap blocked under lock. Expire active If-Match/Step-Up; no invented idempotency. |
| G12 configuration | Registry typed keys, risk/scopability/reason<=2000, nonglobal scope id required. Direct PATCH low-risk/global only: no current low-risk key exists, so action disabled despite supported route metadata. Preview is advisory before/after/risk, not lock/token. Change creation submitted, not applied. Rollback uses first two records in key history across scopes, not selected version; no scoped isolation/freshness promise. Review current key/history and scope before confirmation. |
| G13 change requests | Approve/reject submitted, cancellation submitted/approved requester-only. Apply approved revalidates value. Approval is not apply. No added two-person requirement, no client If-Match. scheduled_for is metadata, no future worker guarantee; UI omits scheduling control. effective_from does not certify gap-free activation. |
| G14 feature flags | Create key/name/type/default variant/risk; PATCH nonretired metadata only. Preview rollout/variant/targets read-only and does not persist targets. Rollout0..100 separate; normal disable keeps percent, emergency disable sets0, retire terminal and0. Enable/retire Step-Up; disable/emergency retain no-Step-Up source metadata. No invented client version. |
| G16 roles | Three GETs supported; role creation/update/permission replacement blocked. No invented editor or successful role mutation. |
| G17 staff | Create name/last name<=100, email or mobile, current password policy; initial enrollment token24h. PATCH only names/employee code. Exact If-Match for traced writes; full active-role replacement and warehouse/physical_store scope replacement revoke sessions. Empty scope per type means GLOBAL, never deny-all. Disable and removal of last superadmin have source checks; no race-free/global guarantee. FIDO reset revokes all keys/sessions, enrollment token30min. Response-only sensitive view, never actual tokens in artifacts/logs/history/URL/browser persistence and no automatic sending. Unknown result requires manual status review, not token fabrication/retry. |
| G21 sessions | Session list up to500 uses account_id; user route id is STAFF_ID, single revoke uses SESSION_ID. Resolve staff independently using permitted staff read or validated known id, never substitute account_id. Lock revokes sessions; unlock only account lock, not inactive staff profile or old sessions. Step-Up commands, no If-Match/idempotency. No last-superadmin protection claim for IAM lock; self-lock risk explicit. |

## State, responsive and accessibility acceptance

Every inherited state maps to a view and disposition. Permission checks precede reads/actions; forbidden operation overrides all surface states. Loading retains focus and clearly marks retained stale data; empty/no results distinct; errors focus summary and link fields. Safe non-sensitive input retained. Version conflict/stale data requires reread and deliberate re-review; no automatic mutation retry. Explicit same key/same payload only for traced idempotency after status check. Success only authoritative response, with secret enrollment result separate. Disabled config action has no current eligible key. Dangerous actions use safe-cancel first, consequences/target review, Step-Up and audit expectations only as traced; no new audit-reading endpoint.

Inherited Step54/B shell at320/360/600/840/1200/1440 for each primary view; other variants320/1440. RTL Persian first, isolated LTR identifiers, integer Toman/no Wallet/no brown. Table card equivalent on narrow widths, logical focus, no fixed obstruction, minimum44px touch (48px frames), error/focus restoration and modal Escape/trap obligations. Long Persian wraps and no required page-level horizontal scroll. Seven targeted browser compositions reviewed: long journal form, both download boundaries, config rollback, global scope warning, confidential enrollment result, blocked RBAC write. All hashes/widths deterministic. Static320 equivalent is not an actual400% zoom, keyboard or screen-reader runtime certification. UI/UX Pro Max focus/confirmation guidance retained; canonical design system unchanged.

## Artifacts and verification

Contract `step56-management-security-wireframes.json`; generator/validator `scripts/*step56-management-security.mjs`; negative regression tests `test/design-system/step56-management-security.spec.ts`; gallery, manifest, per-surface README/traceability and SVGs in `step56-wireframes/G/`. Naming `AD-G-XX--width--view--v1.svg`. Semantic tests reject widened authority, lost traces/guards, false security/retry/version/preview guarantees, direct config enablement, secret-view loss and H progression. Full pnpm verify and git diff --check required. Immutable transport evidence belongs in PR/execution report to avoid self-referential commit hashes. H remains NOT_STARTED.

Frozen counts: actors=3, journeys=4, domains=9, permissions=33, surfaces=34, operations=185, blocked=94, supported=91, states=1571, views=434, frames=1004, artifacts=1075, sources=90.

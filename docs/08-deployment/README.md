# Deployment

Production infrastructure is not considered complete at Step 44. Steps 59–62 cover production topology, PostgreSQL/Redis/API/Worker/Scheduler deployment, storage, SSL, backup/monitoring, staging, hardening and real external-service end-to-end tests.

Current repository contains runnable API/Worker/Scheduler code and development Docker configuration, but deployment credentials must remain outside Git.

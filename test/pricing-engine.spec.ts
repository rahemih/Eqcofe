import test from 'node:test';import assert from 'node:assert/strict';import { PricingEngine, applyPriceChange, roundMoney } from '../src/modules/pricing/domain/pricing-engine';
const engine=new PricingEngine();
test('quantity discount starts at configured threshold 11',()=>{const rule:any={id:'r1',nameFa:'عمده',priority:100,valueType:'percentage',valueNumeric:10,minQuantity:11,maxQuantity:null,customerType:null,stackingPolicy:'best_only'};assert.equal(engine.calculate(1_000_000,[rule],10).finalPriceToman,1_000_000);assert.equal(engine.calculate(1_000_000,[rule],11).finalPriceToman,900_000);});
test('exclusive rule overrides other stacking rules',()=>{const rs:any[]=[{id:'e',nameFa:'e',priority:200,valueType:'percentage',valueNumeric:20,minQuantity:null,maxQuantity:null,customerType:null,stackingPolicy:'exclusive'},{id:'s',nameFa:'s',priority:100,valueType:'percentage',valueNumeric:10,minQuantity:null,maxQuantity:null,customerType:null,stackingPolicy:'stackable'}];const p=engine.calculate(1_000_000,rs,1);assert.equal(p.finalPriceToman,800_000);assert.deepEqual(p.appliedRuleIds,['e']);});
test('best_only selects highest discount',()=>{const rs:any[]=[{id:'a',nameFa:'a',priority:1,valueType:'percentage',valueNumeric:10,minQuantity:null,maxQuantity:null,customerType:null,stackingPolicy:'best_only'},{id:'b',nameFa:'b',priority:1,valueType:'fixed_toman',valueNumeric:150_000,minQuantity:null,maxQuantity:null,customerType:null,stackingPolicy:'best_only'}];assert.equal(engine.calculate(1_000_000,rs,1).finalPriceToman,850_000);});
test('bulk price changes never become negative',()=>{assert.equal(applyPriceChange(100_000,'decrease_fixed',500_000),0);assert.equal(applyPriceChange(100_000,'increase_percent',10),110_000);});
test('currency price rounding uses toman multiple',()=>{assert.equal(roundMoney(1_234_567,10_000),1_230_000);});

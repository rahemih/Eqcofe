import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {spawnSync} from 'node:child_process';
// @ts-ignore Repository-native design validator
import {validateManagement} from '../../scripts/validate-step56-management-security.mjs';
const read=()=>JSON.parse(readFileSync('docs/13-product-design/step56-management-security-wireframes.json','utf8'));
const op=(c:any,key:string)=>c.screens.flatMap((s:any)=>s.operationViews).find((o:any)=>o.operation===key);
test('56-G complete management contract',()=>assert.deepEqual(validateManagement(read()),[]));
const changes:Array<[string,(c:any)=>void]>=[
['surface lost',c=>c.screens.pop()],['source missing',c=>c.sources.pop()],['source forged',c=>c.sources[0].sha256='bad'],['operation lost',c=>c.screens[15].operations.pop()],['permission widened',c=>c.screens[16].permissions.push('admin.all')],['state lost',c=>c.screens[0].states.pop()],['view lost',c=>c.screens[1].views.pop()],['H started',c=>c.nextGateStatus='STARTED'],['runtime changed',c=>c.boundary.runtime=true],['gap lost',c=>c.sourceGaps=[]],['RBAC write enabled',c=>c.screens[15].operationViews[3].executionEnabled=true],['profit preview enabled',c=>op(c,'POST /admin/finance/profit-rules/{id}/preview').executionEnabled=true],['blocked confirmation',c=>c.screens[14].operationViews[0].confirmationView='overview'],['blocked success',c=>c.screens[14].views.find((v:any)=>v.id==='success').kind='summary'],['Step-Up bypass',c=>op(c,'PUT /admin/staff/{id}/scopes').stepUp=false],['emergency Step-Up invented',c=>op(c,'POST /admin/feature-flags/{key}/emergency-disable').stepUp=true],['If-Match removed',c=>op(c,'PUT /admin/staff/{id}/roles').versionRequired=false],['config version invented',c=>op(c,'POST /admin/configuration/{key}/rollback').versionRequired=true],['automatic replay',c=>op(c,'POST /admin/analytics/exports').recovery='AUTO'],['idempotency removed',c=>op(c,'POST /admin/analytics/exports').idempotencyScope=null],['preview applies',c=>op(c,'POST /admin/feature-flags/{key}/preview').confirmation='SAFE_CANCEL_FIRST'],['preview result replaced',c=>op(c,'POST /admin/configuration/{key}/preview').resultView='success'],['direct config enabled',c=>op(c,'PATCH /admin/configuration/{key}').mutationAvailable=true],['empty scope denies',c=>c.invariants.emptyStaffScope='DENY_ALL'],['account id substituted',c=>c.invariants.securityUserRouteId='ACCOUNT_ID'],['global last admin guarantee',c=>c.limitations.globalLastSuperadminLockProtection=true],['racefree guarantee',c=>c.limitations.raceFreeLastSuperadminGuarantee=true],['financial owner guarantee',c=>c.limitations.financeExportOwnerBinding=true],['financial CSV safe claim',c=>c.limitations.financeCsvFormulaNeutralization=true],['scoped rollback guarantee',c=>c.limitations.configurationScopedRollback=true],['scheduled worker claim',c=>c.limitations.configurationScheduledApplyWorker=true],['preview targets persisted',c=>c.limitations.featurePreviewPersistsTargets=true],['journal unbalanced',c=>c.invariants.journalEquation='ANY'],['cost treatment lost',c=>c.invariants.costTreatments.pop()],['profit percent changed',c=>c.invariants.profitPercentSum=99],['tax as money',c=>c.invariants.taxBasisPoints=[0,100]],['secret result replaced',c=>op(c,'POST /admin/staff/{id}/fido/reset-enrollment').resultView='success'],['secret view lost',c=>c.screens[16].views.find((v:any)=>v.id==='reset-enrollment-result').items=[]],['guard hidden',c=>{const o=op(c,'PUT /admin/staff/{id}/scopes');const v=c.screens[16].views.find((v:any)=>v.id===o.confirmationView);v.items=v.items.filter((t:string)=>t!==o.guard);}],['required field lost',c=>op(c,'POST /admin/finance/journals').fields.pop()],['download warning hidden',c=>c.screens[7].views.find((v:any)=>v.id==='op-02').items=[]],['review pending',c=>c.review.visual='PENDING']];
for(const [name,change] of changes)test('56-G rejects '+name,()=>{const c=read();change(c);assert.ok(validateManagement(c).length);});
test('56-G deterministic coverage',()=>{const p=spawnSync(process.execPath,['scripts/generate-step56-management-security.mjs','--check'],{encoding:'utf8'});assert.equal(p.status,0,p.stderr);const m=JSON.parse(readFileSync('docs/13-product-design/step56-wireframes/G/manifest.json','utf8'));assert.equal(m.screenCount,34);assert.equal(m.frameCount,1004);assert.equal(m.artifacts.length+1,1075);});
test('56-G safe cancel before scope replacement',()=>{const svg=readFileSync('docs/13-product-design/step56-wireframes/G/AD-G-17/AD-G-17--320--op-09-confirm--v1.svg','utf8');assert.ok(svg.includes('انصراف'));assert.ok(svg.lastIndexOf('انصراف')<svg.lastIndexOf('جایگزینی'));assert.ok(svg.includes('سراسری'));});

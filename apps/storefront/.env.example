# Stage 58-D server-side API transport configuration.
# These variables are server-only. Do not rename them with a VITE_ prefix.
# The base URL may include an API path prefix but must not contain credentials, query, or hash.
EQCOFE_API_BASE_URL=
EQCOFE_API_TIMEOUT_MS=10000
